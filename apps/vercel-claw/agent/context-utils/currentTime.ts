import { formatCurrentTimeForPrompt, getCurrentTime } from "./getCurrentTime";

export { formatCurrentTimeForPrompt, getCurrentTime };

export const currentTime = getCurrentTime;
export const formatCurrentTime = formatCurrentTimeForPrompt;
