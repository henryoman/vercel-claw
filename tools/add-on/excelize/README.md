# Excelize

This folder is draft reference material for the Excelize Go library.

It is not a shipped runtime integration. It currently includes:

- a one-line install note in `install.sh`
- a minimal workbook example in `code/createdocument.go`
