# Example Tool

This folder is the reference example for the standardized `tools/<tool-id>/index.ts` contract.

Use it as the template when adding a new tool module:

- `index.ts` is the single entrypoint
- `README.md` is the first read target
- supporting docs like `basics.md` or `mcp.json` stay alongside the module
