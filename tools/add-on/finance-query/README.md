# Finance Query

This folder is local reference material for the external Finance Query project.

It is not currently shipped as a runtime tool in `vercel-claw`. Start here for a short summary, then use `README (5).md` for the imported upstream reference covering the hosted API, CLI, GraphQL endpoint, and MCP server.
