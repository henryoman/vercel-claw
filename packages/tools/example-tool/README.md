# Example Tool

This folder shows the standard structure every shipped tool should follow.

- `activate.ts` contains lightweight activation metadata for the CLI/runtime.
- `mcp.json` contains MCP/integration metadata for the tool.
- `skills/` contains optional workflow-oriented docs or prompts for the tool.
- `knowledge/` contains extra docs, references, and tool-specific knowledge files.
